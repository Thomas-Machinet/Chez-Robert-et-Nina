import FrameCornerGold from "./frameCornerGold";

const SocialLinkSegment = () => {
  return (
    <div className="fixed right-0 hidden h-full w-3/12 bg-current xl:flex">
      <FrameCornerGold />
    </div>
  );
};

export default SocialLinkSegment;
